

#ifndef SHAREDMEM_H
#define SHAREDMEM_H

#define SMOB_NAME "/myMemoryObj"
#define SMOBJ_SIZE 200
#define BUFFER_SIZE 10
#define BUFFER_ARCHIVO 4*BUFFER_SIZE
//#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof((arr)[0]))

#endif